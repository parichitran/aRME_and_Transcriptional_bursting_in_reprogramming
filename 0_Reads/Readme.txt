data.csv                                               Contains ftp link for each reads(fastq) 
Download.sh                                            Bash script parses data.csv to get ftp links and download them
